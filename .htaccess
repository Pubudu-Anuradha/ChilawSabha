RewriteEngine On
# Condition to not rewrite filesof the following types
RewriteCond %{REQUEST_URI} !\.(?:css|js|jpe?g|gif|png|woff2?|ttf)$ [NC]
# Rewriting the entire non-empty url as a query string
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]